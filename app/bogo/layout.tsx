// Standalone layout — no Navbar, no Footer, no distractions
// This is a pure conversion page for paid ads

export default function BogoLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
